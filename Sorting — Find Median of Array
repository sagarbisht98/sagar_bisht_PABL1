class Solution:
    def findMedian(self, arr):

        # Sort the array
        arr.sort()

        n = len(arr)

        # If size is odd
        if n % 2 == 1:
            return arr[n // 2]

        # If size is even
        else:
            return (arr[n // 2] + arr[(n // 2) - 1]) / 2
